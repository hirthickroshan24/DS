============================================================
  Q1 - PART 3a: CLASSIFICATION (10 marks) — CO3
============================================================

FILES:
  1. logistic_regression.py  — Logistic Regression (binary classification)
  2. decision_tree.py        — Decision Tree Classifier

WHAT TO STUDY:
  - train_test_split(X, y, test_size=0.2)
  - LogisticRegression() — for binary classification
  - DecisionTreeClassifier(max_depth=5)
  - accuracy_score(y_test, y_pred)
  - confusion_matrix(y_test, y_pred)
  - classification_report(y_test, y_pred)  — precision, recall, f1-score

INFERENCE TIPS:
  - Report accuracy percentage
  - Explain confusion matrix (TP, TN, FP, FN)
  - Discuss precision vs recall
  - Compare models if using both
  - Mention overfitting/underfitting if accuracy is very high/low
============================================================
