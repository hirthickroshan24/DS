============================================================
  Q1 - PART 3b: CLUSTERING (10 marks) — CO3
============================================================

FILES:
  1. kmeans_clustering.py  — KMeans + Agglomerative Clustering

WHAT TO STUDY:
  - MinMaxScaler for scaling before clustering
  - KMeans(n_clusters=k) — Elbow method to find optimal k
  - Elbow plot: inertia vs number of clusters
  - AgglomerativeClustering — Hierarchical clustering
  - Dendrogram — visualize hierarchical merging
  - silhouette_score — evaluate cluster quality (-1 to 1)
  - Scatter plot with cluster labels

INFERENCE TIPS:
  - Explain the elbow method (where the curve bends = optimal k)
  - Report silhouette score (closer to 1 = better)
  - Describe what each cluster represents
  - Compare KMeans vs Agglomerative results
  - Mention why scaling is important for clustering
============================================================
